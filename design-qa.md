# 路线工作台 UI · 方案 2 设计核验

日期：2026-09-05。范围：现有 Electron renderer 的导航壳、规划页层级与路线节点研究区。

## 对照来源与环境

- 视觉权威：`C:/Users/18227/.codex/generated_images/01a07211-2d67-7ab0-8caf-aab513459e8a/exec-3d7ed62a-7fee-42ca-a69a-d2600d368d00.png`，用户选择的第二张方案。
- 浏览器同尺寸截图：`test/artifacts/workbench-desktop-viewport.png`。
- Electron 实际应用截图：`test/artifacts/route-workbench-electron-verified.png`。
- 窄屏截图：`test/artifacts/workbench-mobile.png`。
- 浏览器预览：`http://127.0.0.1:4173/test/preview/index.html`。使用真实 Shell / RouteNodeResearchCard，示例数据与回调隔离，不接触真实来源、模型或旅行数据。
- 参考图和桌面截图均为 1488 × 1056 px；浏览器 CSS viewport 1488 × 1056、devicePixelRatio=1，无密度缩放。浏览器文档宽度 1473px，右侧 15px 为滚动条区域。
- 窄屏 CSS viewport 390 × 844；文档可用宽度 375px，完整截图 375 × 2002px，DPR=1。Electron 还验证了 820px / 320px 窗口宽度。
- 视觉对照状态：云南十日、丽江节点、研究尚未开始、无证据、默认 XHS_STRICT。Electron 夹具进入已选路线的大理节点，另用于确认真实 ChatView 装配和行为。

## 对比结果与迭代

已将参考图与浏览器截图同时打开对照，并复核 Electron 截图。桌面图片的文字与控件在原尺寸输入中可辨识，因此无需额外裁剪区域；另检查了节点栏、主按钮、模式选择、补充表单与右侧说明的 DOM 尺寸和语义。

| 级别 | 早期问题 | 修复与复核 |
| --- | --- | --- |
| P2 | 示例标签继承标题字号，层级不清 | 独立 13px 标签，最终桌面截图可见 |
| P2 | 原有活动导航渐变下划线残留 | 定向覆盖旧伪元素，保留浅紫活动背景 |
| P2 | 主研究区过宽，右侧过窄 | 改为主区 / 38% 约束栏，与参考主要区域比例接近 |
| P2 | 重复副标题、竖排研究模式挤占首屏 | 副标题保留给辅助技术，模式与主按钮并排，压缩说明间距 |
| P2 | 未获取实体时展示不可用确认按钮 | 仅存在实体时展示节点确认；仍由原有状态和操作锁决定可用性 |
| P2 | 手机导航继承全宽按钮，占据七行 | 明确按钮自动宽度，390px 下导航为紧凑换行；页面无横向溢出 |
| P2 | 320px 人工交通补证按钮长文案溢出 | 允许主 / 次按钮换行，Electron 的页面、卡片、路线、表单宽度断言通过 |
| P2 | 手动排序后键盘顺序与视觉顺序不一致 | 主研究区 DOM 位于约束栏前；人工入口排在结果与确认之后 |
| P2 | 预览入口热更新重复 createRoot | 分离 main.tsx 与 Preview 组件，最终新开浏览器页 error / warn 日志为空 |

早期完整对照见 `test/artifacts/workbench-desktop-full.png`；修复后见 `workbench-desktop-viewport.png`。Electron 隐藏窗口从窄屏恢复时曾出现旧像素残留；最终按桌面尺寸重新载入同一隔离夹具并等待绘制，`route-workbench-electron-verified.png` 标题完整。对应几何数据在 `route-workbench-layout.json`，scrollY=0、headerY=0。

## 五项视觉核验

- **字体与层级**：沿用本机 Segoe UI / Microsoft YaHei UI 回退；主标题 29px、研究标题 30px、内容 14–16px、状态与预算辅助文案 12–13px。中文标题、按钮与说明无裁切。辅助数据比概念稿更紧凑，是既有应用密度的保留。
- **间距与布局**：192px 固定导航栏；内容左右留白 42px；横向节点栏；双栏研究区；6–8px 控件圆角。桌面主 CTA y=765px、底部输入区 y=974px，均在 1056px 首屏内。长内容仍可滚动；窄屏右栏自然排在主区之后。
- **色彩与状态**：白色内容面、浅紫侧栏、#5750d9 主色与 #4942c5 强调色；削弱大面积渐变与阴影。使用已有焦点样式和文字状态，状态不依赖颜色单独传达。
- **图片与图标**：本工作台不需要照片。沿用现有 App 品牌 SVG 与导航图标原稿，没有生成替代标志、新增图标包或绘制装饰素材。概念稿中额外的装饰图标不移植；节点圆点是选择控件的状态指示。
- **内容与真实状态**：目的地、日期、晚数、必去点和旅行强度均来自既有 goal/snapshot；不将选中的节点误写为“研究中”。概念稿的重复“示例行程”已简化；示例标记只存在于隔离预览。研究方式与一次性计划详情为实际产品需要保留的信息。

## 功能、响应式与可访问性核验

- 浏览器：节点切换清除旧预览；STANDARD / XHS_STRICT 模式可选择；展开计划后模式锁定；计划显示来源、查询、工具、模型、预算、摘要与过期信息。
- 浏览器：模拟执行仅清空演示计划并明确提示未执行真实调用；已有线索可展开和提交；人工核验表单可展开；输入区可填写并得到本地演示回执。
- 语义：主导航 aria-current=page，当前节点 aria-current=step；按钮、表单标签、details/summary 和跳转到主内容入口沿用语义 HTML。手动草稿回调和节点切换保护保留。
- 390px 浏览器：文档 scrollWidth=clientWidth=375；路线栏内部横向滚动，不造成整页溢出；导航、表单和按钮可用。
- Electron：路线比较、人工交通补证、路线选择、节点研究、会话隔离、未提交草稿保护、320px/820px 布局和键盘聚焦断言通过。
- Electron：旧目的地研究页、来源失败提示、推广阻塞、成员适配、证据冲突裁决和人工清单提交通过。
- 自动检查：317/317 测试通过；lint、TypeScript node/web、生产构建、改动文件格式检查通过。日志在 `.trellis/tasks/09-05-route-workbench-ui/`。
- 最终新开浏览器页 console error/warn：0。Electron 测试使用临时 userData 和本地 IPC 夹具；externalCalls=0、modelCalls=0。

## 保留差异与范围

这是对方案 2 的现有产品实现，不声明像素级复刻。保留既有标志、图标、实际状态文案、研究模式选择及会话功能；会话列表仍在规划内容后方，没有为概念稿中的会话菜单新建数据流。右侧说明不虚构官方核验结果。研究预览和人工路径的原有 IPC、一次性授权与证据身份契约未改。

无待修复的 P0/P1/P2 发现。P3：可在用户后续明确要求时统一其余业务页面的图标密度和卡片细节。本轮不包含所有页面的视觉重做，也不代表真实供应商验收完成。

## 实施清单

- [x] 现有组件接入选定视觉方向
- [x] 真实状态与执行边界保留
- [x] 浏览器桌面 / 窄屏视觉核验
- [x] Electron 实际装配与离线交互验证
- [x] 类型、格式、lint、测试、构建通过
- [x] 预览与交接证据保存

final result: passed
